# 計畫

根據現有的UI進行更新
多標籤同樣要被localstorage & IndexedDB 保存，匯出的結構也需要變更
目前分類資料夾功能保留，分類會依照標籤的骰選過濾其中的內容，如果篩選過後分類資料夾的內容為空的則不顯示
排序篩選優先:多標籤 > 分類

管理收藏:
-收藏管理
    1.新增標籤管理頁面
    2.標籤管理頁面可以新增、編輯、刪除標籤
    3.標籤管理頁面可以新增、編輯、刪除分類
    4.標籤管理頁面可以修改標籤的顏色
-收藏編輯:
    1.每個收藏都能夠新增多個標籤
    2.編輯時使用checkbox選單來同時新增多個標籤，他以下拉可滾動的checkbox 選單
    3.風格根據收藏管理中的配色做為基準
-收藏列表:
  1.標籤在圖片1中紅框的部分水平的顯示，顯示方式類似於圖片3，可以選擇多個標籤來篩選收藏列表
  2.項目顯示的方式調整:
    *標籤以圖片2顯示
    *整體顯示方式分為2行: 1. 名稱 2. 先是分類再顯示標籤。 不用顯示儲存的URL
-新增收藏:
  1.在同一行顯示 先URL > 自訂名稱 > 分類 > 標籤 ，第2行最右邊放置新增按鈕

控制面版的收藏列表:
    1.標籤篩選要在收藏管理按鈕的下方
    2.以下拉checkbox選單顯示
    3.可透過選擇的標籤來顯示對應標籤中的收藏列表

localstorage & IndexedDB 的儲存結構:
預設要有Twitch & Youtube 2個分類，Twitch 以紫色、Youtube 以紅色
tags:[
    {
        name:"標籤名稱",
        color:"標籤顏色",
    }
]

收藏:
[
    {
        name:"收藏名稱",
        url:"收藏url",
        tags:["標籤1","標籤2"],
        category:"分類",
    }
]

## 限制

-不可更改到其他UI介面
