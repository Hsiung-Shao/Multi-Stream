# 載入優化說明

## 優化概述

本次優化實施了以下改進：
1. **統一的 API 載入管理器** - 集中管理所有外部 API 的載入
2. **React.lazy 代碼分割** - 大型組件按需載入
3. **Vite 構建優化** - 手動代碼分割，減少初始包大小
4. **按需載入策略** - 非關鍵功能僅在需要時載入

---

## 必要載入功能（應用啟動時立即載入）

以下功能是應用核心功能，在應用啟動時必須立即載入：

### 1. 核心組件
- ✅ **App.tsx** - 主應用組件
- ✅ **Navbar** - 導航欄（用戶界面核心）
- ✅ **StreamContainer** - 串流容器（核心功能）
- ✅ **ControlPanel** - 控制面板（核心功能）
- ✅ **WelcomeCard** - 歡迎卡片（首屏顯示）

### 2. 播放器 API（必要）
- ✅ **Twitch Player API** (`player.twitch.tv/js/embed/v1.js`)
  - 用途：嵌入 Twitch 直播播放器
  - 載入時機：應用啟動時（`main.tsx`）
  - 載入方式：`apiLoader.loadTwitchPlayerApi()`

- ✅ **YouTube IFrame API** (`youtube.com/iframe_api`)
  - 用途：嵌入 YouTube 直播播放器
  - 載入時機：應用啟動時（`main.tsx`）
  - 載入方式：`apiLoader.loadYouTubePlayerApi()`

### 3. 基礎工具函數
- ✅ **streamUtils** - 串流 URL 解析和驗證
- ✅ **layoutUtils** - 布局管理
- ✅ **chatLayoutUtils** - 聊天室布局管理
- ✅ **i18n** - 國際化支持

### 4. 外部腳本（index.html 中載入）
所有腳本都有被使用，但部分功能已遷移到 React。詳細說明請參考 `index.html` 中的註釋。

- ✅ **config.js** - 配置文件（必要）
- ✅ **js/utils.js** - 工具函數（必要，部分功能已遷移到 React）
  - `getParentDomain/getTwitchParents`: 仍在使用（chat.js、stream.js）
  - `onYouTubeIframeAPIReady`: 仍在使用（apiLoader.ts）
  - `isDraggingStreamBox`: 仍在使用（layout.js、drag-resize.js）
- ✅ **js/security.js** - 安全工具（必要，部分功能已遷移到 React）
  - `escapeHtml/safeJSONParse`: 仍在使用（settings.js 中大量使用）
  - `validateUrl`: 已遷移到 React，但保留以兼容
- ✅ **js/main.js** - 主入口腳本（必要）
  - 初始化全局變數：`window.streamCount`、`window.players`、`window.streamData`
- ✅ **js/twitch-api.js** - Twitch 數據 API（必要-按需載入）
- ✅ **js/stream.js** - 串流管理（部分必要-向後兼容）
  - 主要功能已遷移到 React，但保留以向後兼容
- ✅ **js/settings.js** - 設定和收藏管理（必要）
  - 包含 `window.favoriteStreams`、`window.youtubeApiUtils` 等
- ✅ **js/chat.js** - 聊天室功能（必要）
  - `window.createChat()`、`window.toggleChat()`、`window.setupChatResizer()`
- ✅ **js/volume.js** - 音量控制（必要）
  - `window.applyMasterVolumeToStream()`、`window.updateMasterVolume()`
- ✅ **js/control-panel.js** - 控制面板腳本（部分必要）
  - `updateStreamOrderList()`: 仍在使用
  - `toggleControlPanel()`: 已遷移到 React

---

## 按需載入功能（僅在需要時載入）

以下功能使用 React.lazy 進行代碼分割，僅在用戶觸發相關操作時才載入：

### 1. 懶加載組件
- 🔄 **VersionHistory** - 版本歷史
  - 觸發時機：用戶點擊「版本歷史」按鈕
  - 載入方式：`React.lazy(() => import('./components/VersionHistory'))`

- 🔄 **Tutorial** - 教學指南
  - 觸發時機：用戶點擊「教學」按鈕
  - 載入方式：`React.lazy(() => import('./components/Tutorial'))`

- 🔄 **FavoritesManager** - 收藏管理器
  - 觸發時機：用戶點擊「收藏」按鈕
  - 載入方式：`React.lazy(() => import('./components/FavoritesManager'))`
  - 備註：這是大型組件（1448 行），延遲載入可顯著減少初始載入時間

- 🔄 **AboutPage** - 關於頁面
  - 觸發時機：用戶點擊「關於」按鈕
  - 載入方式：`React.lazy(() => import('./components/AboutPage'))`

- 🔄 **PrivacyPage** - 隱私權政策頁面
  - 觸發時機：用戶點擊「隱私權政策」連結
  - 載入方式：`React.lazy(() => import('./components/PrivacyPage'))`

### 2. 數據 API（按需載入）
- 🔄 **Twitch 數據 API** (`window.twitchApi`)
  - 用途：搜尋 Twitch 頻道、查詢開台狀態
  - 載入時機：用戶嘗試搜尋頻道時
  - 載入方式：`apiLoader.loadTwitchDataApi()`
  - 備註：通過 `js/twitch-api.js` 腳本載入，此函數只檢查是否可用

- 🔄 **YouTube 數據 API** (`window.youtubeApiUtils`)
  - 用途：搜尋 YouTube 頻道、查詢開台狀態、頻道驗證
  - 載入時機：用戶嘗試搜尋或驗證 YouTube 頻道時
  - 載入方式：`apiLoader.loadYouTubeDataApi()`
  - 備註：通過 `js/settings.js` 腳本載入，此函數只檢查是否可用

### 3. 播放器 API（按需載入）
雖然播放器 API 在應用啟動時載入，但在 `StreamBox` 組件中也有按需載入邏輯：
- 🔄 **Twitch Player API** - 在創建 Twitch 播放器前檢查並載入
- 🔄 **YouTube Player API** - 在創建 YouTube 播放器前檢查並載入

---

## 優化效果

### 預期改進
1. **初始載入時間**：減少 40-60%
2. **首屏渲染時間**：減少 30-50%
3. **初始包大小**：減少約 200-300 KB（通過代碼分割）
4. **用戶體驗**：非關鍵功能按需載入，不影響核心功能使用

### 代碼分割策略
- **vendor-radix**：Radix UI 組件庫（約 150 KB）
- **vendor-mui**：Material-UI 組件庫（約 200 KB）
- **vendor-icons**：Lucide React 圖標庫（約 50 KB）
- **lazy-components**：懶加載組件（約 100-150 KB，按需載入）

---

## API 載入管理器使用說明

### 基本用法

```typescript
import { apiLoader } from './utils/apiLoader';

// 載入播放器 API（必要，應用啟動時）
await apiLoader.loadAllPlayerApis();

// 載入單個播放器 API
await apiLoader.loadTwitchPlayerApi();
await apiLoader.loadYouTubePlayerApi();

// 載入數據 API（按需）
await apiLoader.loadTwitchDataApi();
await apiLoader.loadYouTubeDataApi();

// 載入所有數據 API
await apiLoader.loadAllDataApis();

// 檢查 API 狀態
const status = apiLoader.getStatus();
const isLoaded = apiLoader.isLoaded('twitchPlayer');
```

### API 狀態
- `idle` - 尚未開始載入
- `loading` - 正在載入中
- `loaded` - 已成功載入
- `error` - 載入失敗

---

## 注意事項

1. **播放器 API** 必須在應用啟動時載入，因為這是核心功能
2. **數據 API** 可以按需載入，但建議在用戶可能使用搜尋功能前預載入
3. **懶加載組件** 使用 `Suspense` 包裝，提供載入中的回退 UI
4. **向後兼容**：保留了原有的 `loadPlayerApis.ts` 函數，但實際調用新的 `apiLoader`
5. **外部腳本優化**：
   - 所有腳本都有被使用，因此都保留載入
   - 在 `index.html` 和腳本文件中添加了詳細註釋，說明哪些功能已遷移到 React
   - 部分功能已遷移到 React，但保留舊腳本以向後兼容
   - 未來可以逐步將剩餘功能遷移到 React，然後移除舊腳本

---

## 文件結構

```
src/
├── utils/
│   ├── apiLoader.ts          # 統一的 API 載入管理器（新增）
│   └── loadPlayerApis.ts     # 播放器 API 載入（已更新，使用 apiLoader）
├── components/
│   ├── Navbar.tsx            # 必要載入
│   ├── StreamContainer.tsx   # 必要載入
│   ├── ControlPanel.tsx      # 必要載入
│   ├── WelcomeCard.tsx       # 必要載入
│   ├── VersionHistory.tsx    # 懶加載
│   ├── Tutorial.tsx          # 懶加載
│   ├── FavoritesManager.tsx  # 懶加載
│   ├── AboutPage.tsx         # 懶加載
│   └── PrivacyPage.tsx       # 懶加載
└── App.tsx                   # 主應用（已更新，使用懶加載）
```

---

## 測試建議

1. 檢查初始載入時是否只載入必要組件
2. 驗證懶加載組件是否在觸發時才載入
3. 確認 API 載入狀態管理是否正確
4. 測試網路慢速環境下的載入體驗
5. 使用瀏覽器開發工具檢查代碼分割效果

